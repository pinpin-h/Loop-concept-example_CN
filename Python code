#計數器：使用for循環，打印從1到10的數字。
for a in range(1,11):  #用range語法來定義數字範圍，從1開始，但不包括11
    print(a)

#尋找數字：使用while循環，從列表中尋找特定的數字，當找到該數字時停止循環
x = 0 #定義x起始值
while x <= 21:
    print(x)
    x += 1  # +=表示每做一個迴圈，就把x+1後帶入,直到條件被滿足，來避免永無止境的迴圈
